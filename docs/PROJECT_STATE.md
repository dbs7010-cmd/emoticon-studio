# PROJECT STATE

## LOCKED
- `docs/MASTER_SOURCE.md` 전체
- 핵심 리듬: `그린다 → 저장 → 다음 칸 → 반복 → 1세트 완성`
- 초보 성인 상업 이모티콘 제작툴이며 범용 Painter/어린이 스티커 앱이 아니다.

## DONE — 기존 V1
- Expo SDK 57 모바일 앱
- 로컬 프로젝트/32 슬롯
- 원캐릭터 기준 이미지
- 기본 Painter
- PNG 저장/공유
- 기본 QA
- 로컬 이중 저장/복구
- 4열 결과 슬롯 보드
- 슬롯 선택 → Painter 직행

## CURRENT
실기기 1차 피드백 반영: Painter 자체 기능보다 `상업 이모티콘 1세트를 연속 제작한다`는 의도를 화면과 조작에 드러내는 수정.

### 실기기에서 확인된 문제
- 영어 레이어 이름이 작업 흐름을 방해함
- 참고 이미지 자체를 캔버스에서 직관적으로 이동/확대하기 어려움
- 지우개가 참고 레이어 선택 상태에서 불안정
- 색상 선택 폭 부족
- Undo/Redo 버튼이 화면을 차지함
- 레이어가 하단에 묻혀 일반 드로잉 앱보다도 불편함
- Painter 화면이 `이모티콘 세트 연속 제작`보다 범용 그리기 도구처럼 보임
- Painter의 대사 추천 버튼은 불필요

### 이번 수정
- Painter 모든 주요 레이어 표시를 한국어 `그림 1/2`, `참고 1/2...`로 변경
- 상단에 `이모티콘 세트 01/32`와 레이어 버튼 배치
- Painter 대사 추천 버튼 제거
- 참고 썸네일 선택 후 캔버스에서 두 손가락 이동/핀치로 참고 이미지 자체 transform
- 펜/지우개 선택 시 마지막 그림 레이어로 자동 복귀해 지우개/그리기 대상 오류 방지
- 두 손가락 탭 = 되돌리기, 세 손가락 탭 = 다시 실행
- Undo/Redo 버튼 제거
- 색상 팔레트 24색으로 확장 + 직접 HEX 입력 유지
- 레이어 패널을 캔버스 상단에서 열도록 변경
- 하단을 `따라 그릴 참고 → 그림 도구 → 저장/다음 칸` 순서로 정리
- 원캐릭터 표기를 한국어로 변경
- 저장 → 다음 칸 연속 작업 유지

## VERIFICATION STATUS
- 이전 실기기 버전에서 슬롯 저장/썸네일/다음 슬롯/공용 참고 흐름은 작동 확인
- `npm run test:ux`는 이전 수정에서 PASS 확인
- 최신 수정은 GitHub 브랜치에 반영 완료
- 현재 실행 환경은 외부 GitHub/npm 네트워크 실행이 불가하여 최신 typecheck/test/bundle 재실행은 미검증
- 최신 touch gesture/지우개/참고 transform은 Android Expo Go 재검증 필요

## CURRENT ACCEPTANCE CHECK
1. 슬롯 → Painter 직행
2. 상단에서 `이모티콘 세트 01/32`가 명확히 보임
3. 영어 Draw Layer/Reference가 사용자 UI에 노출되지 않음
4. 참고 썸네일 탭 → 참고 활성화
5. 활성 참고를 캔버스에서 두 손가락으로 이동/확대
6. 펜/지우개 누르면 그림 레이어로 자동 복귀
7. 지우개가 선택 그림 레이어만 정상 삭제
8. 두 손가락 짧은 탭 → 되돌리기
9. 세 손가락 짧은 탭 → 다시 실행
10. 두 손가락 이동/핀치는 Undo로 오인되지 않음
11. 레이어 버튼이 상단에서 즉시 접근 가능
12. 색상 팔레트가 충분히 보임
13. 저장 → 현재 슬롯 썸네일 반영
14. 저장 → 다음 칸 → 동일 작업 흐름 유지
15. 다음 칸에서도 공용 참고 이미지 유지
16. 앱 재실행 후 작업/레이어/참고 상태 유지

## NEXT
최신 브랜치를 폰에서 위 16개만 다시 검증한다.
발생한 실제 문제만 수정하고 P0 Painter 흐름이 안정되기 전에는 AI 고도화/움직이는 이모티콘/PRO 기능을 추가하지 않는다.

## BLOCKED
- 최신 수정의 Android 실기기 사용감 재검증
